// src/features/catalog/utils/productClassifier.js
export class ProductClassifier {
  static extractCategory(title) {
    // Extract "Jackets", "Shirts", etc. from titles
  }
  
  static extractStyle(title) {
    // Extract "CRUISER", "JAC-SHIRT", etc.
  }
  
  static buildHierarchy(products) {
    // Build your category → style → size → color hierarchy
  }
}